# iWarehouse API in Typescript

iWarehouse Application Platform Interface en Typescript

## Installation

Use the package manager [npm](https://www.npmjs.com/) to install dependencies.

```bash
npm install